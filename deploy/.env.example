# Database
DB_PASSWORD=your-secure-db-password-here

# MinIO (S3-compatible storage)
MINIO_ACCESS_KEY=gitclaw-access-key
MINIO_SECRET_KEY=your-secure-minio-secret-here

# Admin credentials
ADMIN_USERNAME=admin
# Generate with: echo -n "your-password" | sha256sum | cut -d' ' -f1
ADMIN_PASSWORD_HASH=your-sha256-hash-here

# Domain (for SSL)
DOMAIN=gitclaw.dev
